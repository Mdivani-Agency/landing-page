{
  "name": "mdio-landing-page",
  "install": "corepack enable && yarn install --immutable",
  "terminals": [
    {
      "name": "dev-server",
      "command": "corepack enable && yarn dev",
      "description": "Next.js App Router dev server with hot reload at http://localhost:3000."
    }
  ],
  "ports": [
    { "name": "next dev server", "port": 3000 }
  ]
}
