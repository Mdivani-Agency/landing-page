---
description: Rules for gradually migrating the landing page to Next.js
globs: ["app/**/*", "pages/**/*", "components/**/*", "src/**/*"]
alwaysApply: false
---

# Next.js Migration Rules

- Prefer small, incremental migrations over rewriting the entire application.
- Preserve existing URLs, metadata, content, and visual behavior.
- Use Server Components by default where applicable.
- Add `"use client"` only when client-side state, effects, browser APIs, or event handlers are required.
- Keep reusable UI in `components/`.
- Keep page-specific content and logic close to the relevant route.
- Use Next.js `Image` for optimized images when appropriate.
- Use `Link` for internal navigation.
- Keep secrets and private environment variables server-side.
- Use environment variables for configuration; never hardcode credentials.
- Avoid mixing routing or data-fetching patterns without a clear reason.
- Ensure migrated pages remain accessible, responsive, and SEO-friendly.
- Do not migrate unrelated sections unless explicitly requested.
